<!doctype html>
<html lang="pt-BR">
<head>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width,initial-scale=1" />
	<title><%= product.name %></title>
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
	<style>
		:root {
    --primary: #5d46f4;
    --primary-hover: #4a38c7;
    --bg: #f8fafc;
    --text-main: #1e293b;
    --text-muted: #64748b;
}

body { 
    font-family: 'Inter', system-ui, -apple-system, sans-serif; 
    margin: 0; 
    padding: 20px; 
    color: var(--text-main); 
    background-color: var(--bg);
    line-height: 1.5;
}

.container {
    max-width: 900px;
    margin: 40px auto;
}

.back-link { 
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--text-muted); 
    text-decoration: none; 
    font-size: 14px;
    margin-bottom: 20px;
    transition: color 0.2s;
}

.back-link:hover { color: var(--primary); }

h1 { font-size: 1.8rem; margin-bottom: 30px; font-weight: 800; }

.grid { 
    display: grid; 
    grid-template-columns: 1fr 1fr; 
    gap: 24px; 
}

@media (max-width: 768px) {
    .grid { grid-template-columns: 1fr; }
}

.card { 
    background: white;
    border: 1px solid #e2e8f0; 
    border-radius: 16px; 
    padding: 24px; 
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
}

.card-title {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: 700; 
    margin-bottom: 16px; 
    font-size: 1.1rem;
    color: var(--text-main);
}

.price-tag {
    background: #f1f0ff;
    color: var(--primary);
    padding: 12px;
    border-radius: 12px;
    font-size: 1.5rem;
    font-weight: 800;
    margin-top: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

label { font-weight: 600; display: block; margin-top: 16px; font-size: 14px; color: var(--text-main); }

input[type="email"], input[type="text"] { 
    width: 100%; 
    padding: 12px; 
    border: 2px solid #e2e8f0; 
    border-radius: 10px; 
    margin-top: 6px; 
    box-sizing: border-box;
    font-size: 16px;
    transition: border-color 0.2s;
}

input:focus {
    outline: none;
    border-color: var(--primary);
}

button { 
    width: 100%;
    background: var(--primary); 
    color: white; 
    padding: 14px; 
    border: 0; 
    border-radius: 10px; 
    cursor: pointer; 
    margin-top: 20px; 
    font-weight: 700;
    font-size: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    transition: background 0.2s, transform 0.1s;
}

button:hover { background: var(--primary-hover); }
button:active { transform: scale(0.98); }

.small { font-size: 12px; color: var(--text-muted); line-height: 1.4; }

.secure-badge {
    margin-top: 20px;
    display: flex;
    align-items: center;
    gap: 8px;
    color: #10b981;
    font-size: 13px;
    font-weight: 600;
}
	</style>
</head>
<body>
    <div class="container">
        <a class="back-link" onclick="window.history.go(-1);"><i class="fa-solid fa-arrow-left"></i> Voltar para a loja</a>
        
        <h1>Finalizar sua Compra</h1>

        <div class="grid">
            <div class="card">
                <div class="card-title">
                    <i class="fa-solid fa-box-open" style="color: var(--primary)"></i> 
                    Resumo do Pedido
                </div>
				<img src="/images/<%= product.image %>" style="width: 100%;">
                <p style="color: var(--text-muted);"><%= product.description %></p>
                
                <div class="price-tag">
                    <span style="font-size: 14px; font-weight: 600; color: var(--text-muted);">Total:</span>
                    R$ <%= product.price.toFixed(2) %>
                </div>

                <div class="secure-badge">
                    <i class="fa-solid fa-shield-halved"></i>
                    Pagamento 100% Processado com Segurança
                </div>
            </div>

            <div class="card">
                <form method="post" action="/buy/<%= product.id %>">
                    <div class="card-title">
                        <i class="fa-solid fa-user-check" style="color: var(--primary)"></i> 
                        Seus Dados
                    </div>
                    
                    <label for="email">E-mail para entrega</label>
                    <input id="email" name="email" type="email" placeholder="onde você receberá o produto" required />
                    
                    <p class="small" style="margin-top:12px;">
                        <i class="fa-solid fa-circle-info"></i>
                        <% if (typeof requireCpf !== 'undefined' && requireCpf) { %>
                            Sua conta exige CPF para PIX em produção.
                        <% } else { %>
                            O acesso será enviado para o e-mail acima logo após a confirmação do PIX.
                        <% } %>
                    </p>

                    <button type="submit">
                        Pagar com PIX
                        <i class="fa-solid fa-chevron-right" style="font-size: 12px;"></i>
                    </button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>


